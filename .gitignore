.DS_Store
node_modules/
.yarn/install-state.gz
.pnp.*
dist/
coverage/
contracts/dist/
*.tsbuildinfo
*.log
.env
.env.*
!.env.example
backend/.env
backend/.env.*
!backend/.env.example

# oh-my-codex local runtime state
.omx/
.cmax/

# Swift contract check
contracts/swift-client-check/.build/
contracts/swift-client-check/Sources/CountryFlagsAPI/openapi.yaml
contracts/swift-client-check/Package.resolved

# iOS
ios/Config/Local.xcconfig
ios/build/
# `xcodebuild -derivedDataPath build` from the repository root lands here, and
# a device build fills it with well over a gigabyte no one wants staged.
/build/
ios/**/*.xcresult
ios/CountryFlagsKit/.build/
ios/CountryFlags.xcodeproj/project.xcworkspace/xcuserdata/
# Package.resolved pins the dependency versions and is committed.
ios/CountryFlags.xcodeproj/xcuserdata/
xcuserdata/
DerivedData/

# Playwright output
admin/test-results/
admin/playwright-report/

# Store screenshots are rebuilt by ios/Scripts/capture-screenshots.sh,
# never committed: the copy beside them is what is versioned.
ios/StoreMetadata/screenshots/
