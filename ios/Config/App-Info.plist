<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  The app target ships an explicit Info.plist instead of a generated one:
  GENERATE_INFOPLIST_FILE only understands the INFOPLIST_KEY_ settings Xcode
  knows about and silently drops application specific keys, which left the app
  without its environment at runtime.

  Values come from the selected xcconfig; none of them is a secret.
-->
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<!--
	  The strings live in the package bundle, so the app bundle itself carries
	  no .lproj directory. Without this list iOS treats the app as English only:
	  Russian never wins language negotiation and the app does not appear in the
	  per-app language picker.
	-->
	<key>CFBundleLocalizations</key>
	<array>
		<string>en</string>
		<string>ru</string>
	</array>
	<key>CFBundleDisplayName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>CFAppEnvironment</key>
	<string>$(CF_APP_ENVIRONMENT)</string>
	<key>CFAPIBaseURL</key>
	<string>$(CF_API_BASE_URL)</string>
	<key>CFDeepLinkScheme</key>
	<string>$(CF_DEEP_LINK_SCHEME)</string>
	<key>CFGoogleClientID</key>
	<string>$(CF_GOOGLE_IOS_CLIENT_ID)</string>
	<key>CFGoogleServerClientID</key>
	<string>$(CF_GOOGLE_SERVER_CLIENT_ID)</string>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>$(CF_DEEP_LINK_SCHEME)</string>
			</array>
		</dict>
		<!-- Google's redirect back into the app. Registers nothing while the
		     scheme is empty. -->
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>$(PRODUCT_BUNDLE_IDENTIFIER).google</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>$(CF_GOOGLE_URL_SCHEME)</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
